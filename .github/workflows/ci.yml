name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore ClosedXmlTutorial/ClosedXmlTutorial.slnx

      - name: Build
        run: dotnet build ClosedXmlTutorial/ClosedXmlTutorial.slnx --no-restore

      - name: Test
        run: dotnet test ClosedXmlTutorial/ClosedXmlTutorial.slnx --no-build --verbosity normal
